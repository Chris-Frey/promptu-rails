<% if user_signed_in? %>
  <div class="header-body">
    <div class="menu">
    <p><%= link_to "Logo Here", index_activity_path  %></p>
    <p><%= link_to "Home", index_activity_path  %></p>
      <p><%= link_to 'Profile', user_show_path(current_user.id), data: { turbo_method: :get } %> </p>
      <p><%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to log out?" } %> </p>
    </div>
  </div>
<%end%>