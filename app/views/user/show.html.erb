
  <h1>Profile</h1>
  <h2> <%= current_user.name %> </h2>
  <h2> <%= current_user.username %> </h2>
  <h2> <%= current_user.email %> </h2>
  <h2> <%= current_user.id %> </h2>
  <% me = current_user.id %>
  <h2> <% Activity.all.each do |activity| %> </h2>
        <ul>
          <li class="activity_list"><%= activity.name if activity.user_id === current_user.id %></li>
        </ul>
      <% end %>
  <br>
  <%= button_to "Home", activities_path, method: :get %>